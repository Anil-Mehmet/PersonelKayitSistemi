@{
    ViewData["Title"] = "Admin Girişi";
    ViewData["ActivePage"] = "AdminLogin";
    Layout = "~/Views/Shared/_LayoutAuth.cshtml";
}

@if (ViewBag.Hata != null)
{
    <div class="alert alert-danger d-flex align-items-center gap-2" role="alert">
        <i class="bi bi-exclamation-circle-fill"></i>
        <span>@ViewBag.Hata</span>
    </div>
}

<form method="post" class="auth-form">
    <div class="form-group mb-3">
        <label class="form-label">E-posta</label>
        <div class="input-with-icon">
            <i class="bi bi-envelope"></i>
            <input type="email" name="email" class="form-control" placeholder="admin@sirket.com" required autofocus />
        </div>
    </div>

    <div class="form-group mb-4">
        <label class="form-label">Şifre</label>
        <div class="input-with-icon">
            <i class="bi bi-lock"></i>
            <input type="password" name="sifre" class="form-control" placeholder="••••••••" required />
        </div>
    </div>

    <button type="submit" class="btn btn-primary w-100 auth-submit auth-submit-admin">
        Giriş Yap
        <i class="bi bi-arrow-right ms-1"></i>
    </button>
</form>

<a href="/Login" class="auth-back-link">
    <i class="bi bi-arrow-left me-1"></i>Personel girişine dön
</a>